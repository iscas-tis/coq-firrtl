(env
  (dev
    (flags         (:standard -w +42)))
  (release
    (flags         (:standard -O3))))

(executables
  (names            printmlir);transhiast_pair nodehelper hipparser printmlir transhiast transast1 pparser inline
  (libraries        hifirrtl_lang);interpreter ocamlgraph unix
)
