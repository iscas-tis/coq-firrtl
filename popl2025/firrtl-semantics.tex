%!TEX root = main.tex

\subsection{Module graphs}

%For simplicity, let us focus on a HiFIRRTL circuit $A$ that contains no ``inst of'' statements, that is, circuit $A$ contains exactly one module $A$.

%We introduce a structure called \emph{circuit diagrams} to formalize the syntax of HiFIRRTL, in particular, to represent the high-level constructs of HiFIRRTL.   

% by reading https://github.com/iscas-tis/coq-firrtl/blob/with_implicits/src/ModuleGraph_oriented.v



%A module graph consists of two things: vertex and edges. 
%
%vertex can be output, inport, register, wire, node.  (??)
%
%components have types


%For each module, we have ports

%connection: for each input connector of a module graph, there is an associated expression, which is based on the vertices in the module graph. 

%Input connectors are identified by a triple of natural numbers:
%- a pair to identify the vertex of the module graph
%- a number to identify the input connector of the vertex.

\newcommand{\clock}{{\tt Clock}\xspace}
%\newcommand{\UInt}{{\tt UInt}\xspace}
\newcommand{\UInt}[1]{{\tt UInt}$\langle #1\rangle$\xspace}
\newcommand{\sorts}{{\sf Sorts}}
\newcommand{\width}{{\sf width}}
\newcommand{\datatype}{{\sf type}}
\newcommand{\vtypes}{{\sf VTypes}}

\newcommand\inport{\sf InPort}
\newcommand\outport{\sf OutPort}
\newcommand{\register}{{\sf Register}}
\newcommand{\node}{{\sf Node}}
\newcommand{\wire}{{\sf Wire}}
\newcommand{\expr}{{\sf expr}}
\newcommand{\inputport}{\mathcal{I}}
\newcommand{\outputport}{\mathcal{O}}

\newcommand{\exps}{{\sf Exps}}
\newcommand{\refs}{{\sf Refs}}

\newcommand{\pfun}{\rightharpoonup}


%We fix ground types \clock and  \UInt{n} where $n$ is a positive integer. 
%based on the discussion with zhilin, aggregate type is not needed. 
%, and aggregate types consisting of vector type and bundle type. Typically, we write \UInt{n}. 


%Let us fix a LoFIRRTL circuit $A$ that is free of \prettyll{inst of} statements. According to the syntax of LoFIRRTL, the top-level module of the circuit must be module $A$.

%We first introduce some notations.


%A \emph{generalized connect statement} is a statement of the form $z <= e$ or $\mbox{\tt \prettyll{node}}\ z = e$, where $z$ and $e$ are called the \emph{head} and \emph{body} of the generalized connect statement, respectively. Intuitively, a generalized connect statement is either a connect statement $z <= e$ or a node-declaration statement $\mbox{\tt \prettyll{node}}\ z = e$.

%We denote by $\iports(A)$, $\oports(A)$, $\wires(A)$, $\nodes(A)$
%and $\registers(A)$ the sets of input ports, output ports, wires, nodes and registers of a module $A$, respectively.
%Furthermore, we use $\ports(A)$ to denote the set of ports $\iports(A) \uplus \oports(A)$,
%$\cpnts(A)$ to denote the set of components $\ports(A) \uplus \wires(A) \uplus \nodes(A) \uplus \registers(A)$,
%and $\ccpnts(A)$ to denote the set of components $\oports(A) \uplus \wires(A) \uplus \nodes(A)$ that must be connected to, % in the module $A$,
%where a component $z$ is connected to via a generalized connect statement whose head is $z$.
%Note that the input ports $\iports(A)$ are not connected to, moreover, the registers may not be connected to, thus they
%are not included in $\ccpnts(A)$. (If a register is not connected to, then it keeps the current value.)

%
We denote by $\gtypes$ the set of ground types $(\{\mbox{\prettyll{UInt}}, \mbox{\prettyll{SInt}}\} \times \Nat) \cup \{\mbox{\prettyll{Clock}},
\mbox{\prettyll{Reset}}, \mbox{\prettyll{AsyncReset}}\}$ and let $\bv=\{0,1\}^+$, i.e., the set of bit vectors.
%
For each $t = (\tau, n) \in \{\mbox{\prettyll{UInt}}, \mbox{\prettyll{SInt}}\} \times \Nat$, $n$ is the \emph{width} of $t$, denoted by $wd(t)$.

Let us fix a {\hifirrtl} module $M$. We assume that $M = (pp, ss)$, where $pp$ is a sequence of port declarations and $ss$ is a sequence of statements. 
Moreover, for convenience, we assume that an \emph{identifier function} $id$ exists, which maps from the name of a component in $M$ to a natural number. Note that $id$ is an injective function. Later on, we always use identifiers (instead of names) to refer to the components in $M$. 

In the sequel, we define a concept of \emph{module graphs} to define the semantics of {\hifirrtl} programs. Intuitively, a module graph $G$ for $M$ represents an instantiation of $M$, where all the implicit widths are made explicit. Moreover, the vertices of $G$ represent the components of ground type that are obtained from $M$ by flattening the components of aggregate type. 

As a result, the vertices of a module graph for $M$ are of the form $(a, o) \in \Nat \times \Nat$, where  
$(a, o)$ is one of the following forms, 
%each component $cnt$ of ground type in $M$ is associated with an identifier $(a, o)$, where
\begin{itemize}
\item $a = id(cnt)$ such that $cnt$ is a component of vector type, and $o$ is an index in the vector, 
% 
\item $a = id(cnt)$ such that $cnt$ is a component of bundle type, and $o$ is the offset of a field of $cnt$ of ground type, (For a component of bundle type, the offsets of its fields of ground type are obtained by a depth-first search over its fields. For instance, for the bundle type \prettyll{output io : {flip enable : UInt<1>, calc : UInt<3>}}, the offsets of \prettyll{enable} and \prettyll{calc} are $0$ and $1$ respectively.)
%is a sub-component of some other component $cnt'$ of an aggregate type, then $a \in \Nat$ is the identifier of $cnt'$ \tl{there is a bit issue here as the identifier of $cnt'$ is a pair?} and $o$ represents the offset of $cnt$ in $cnt'$, 
\item $a = id(cnt)$ such that $cnt$ is a component of ground type, and $o = 0$.
\end{itemize}

Let $\idset_M$ denote the set of pairs $(a, o)$ that is obtained from $M$. 

%For convenience, we define an \emph{identifier} as a pair $(a, o) \in \Nat \times \Nat$. %where intuitively $a$ refers to ... and $o$ refers to ... . 
%Let $\idset$ denote the set of identifiers. Intuitively, 
%each component $cnt$ of ground type in $M$ is associated with an identifier $(a, o)$, where
%\begin{itemize}
%\item if $cnt$ is a sub-component of some other component $cnt'$ of an aggregate type, then $a \in \Nat$ is the identifier of $cnt'$ \tl{there is a bit issue here as the identifier of $cnt'$ is a pair?} and $o$ represents the offset of $cnt$ in $cnt'$, 
%\item otherwise, $a \in \Nat$ is the identifier of $cnt$ and $o = 0$.
%\end{itemize}


%Moreover, for the components where aggregate types are nested, the offsets of the components of ground types are determined by the traversal order of a depth-first search. \tl{to be improved.}

We consider a finite set $\sorts = \{ \inport, \outport, \wire, \node, \register\}$ of \emph{sorts}.
$\exps$ (resp. $\refs$) denotes the set of {\hifirrtl} expressions (resp. references), that is, %the set of strings that can be 
those derived  by the rules from $e$ (resp. $r$) in Figure~\ref{fig-firrtl-synt-simp}. 

%consisting of \register, \node, \wire, \inport, and \outport. Let 

A \emph{vertex type} is either \tl{the name "type" is overloaded, can be change to something else?}
\begin{itemize}
\item $(s, t)$ with $s \in \{\inport, \outport, \wire, \node\}$ and $t \in \gtypes$,  
\item or $(\register, t, e_1, e_2, e_3, b)$ such that $t \in \gtypes$, $e_1, e_2, e_3 \in \exps$ and $b\in \{0,1\}$. (Intuitively, $e_1$ represents a clock, $e_2$ represents a reset clock, and $e_3$ represents the reset value, and $b$ is a Boolean value  that denotes whether the reset is asynchronous or not).  
\end{itemize}
Let $\vtypes$ denote the set of vertex types. 

A \emph{vertex map} is a function $vm: \idset_M \rightarrow \vtypes$ with a finite domain. 

A \emph{connection tree} is a function $ct: \idset_M \rightarrow \exps$ with a finite domain. 

%A \emph{vertex} is a pair $\idset \times \vtypes$.

\begin{definition}[Module graph]\label{def-mg}
	A \emph{module graph} $G$ is a pair $(vm, ct)$ where $vm$ is a vertex map and $ct$ is a connection tree. 
\end{definition}
\tl{this def has little to do with graph!?}\zhilin{if $ct(id)$ is a component of ground type, then it is analogous to the graph edges, otherwise, $ct(id)$ may involve multiple identifiers, somehow it can be seen as an edge in a supergraph. May changed to module diagram}

%Intuitively, a module graph is a representation of a module, where the focus is on the components of ground types, all implicit widths are concretized, $vm$ records the declarations of components, and $ct$ records for each component an expression that is generated from its connect statements according to the last-connect semantics. 

\begin{example}
An example of module graphs here
\end{example}

\zhilin{stopped here}

tmap is constructed from pp, ss, vm

Let $G= (vm, ct)$ be a module graph. 



For a statement $\kappa\ v: t$ where $\kappa =\mbox{\prettyll{input},\prettyll{output},\prettyll{wire}}$, 
we define $type_{vm, v}(t)$ inductively as follows.
\begin{itemize}
\item $type_{vm,v}(\tau) = \tau$ for each $\tau \in \{\mbox{\prettyll{Clock}}, \mbox{\prettyll{Reset}}, \mbox{\prettyll{AsyncReset}}\}$, 
%
\item $type_{vm, v}(\mbox{\prettyll{UInt}}) = \mbox{\prettyll{UInt}}(vm(id(v)))$ and $type_{vm,v}(\mbox{\prettyll{SInt}}) = \mbox{\prettyll{SInt}}(vm(id(v)))$.
\end{itemize}

We shall construct from $pp, ss, vm$ a function $tmap$ that assigns to each component (including the components of aggregate types) an $ftype$ where all the widths are explicit.

\begin{table}[ht]
  \small
  \begin{gather*}
    \infer[skip]
    {\seqq{(te, s, rs) \xrightarrow{\mbox{skip}} (te, s, rs)}}
    {\seqq{-}}
    \quad
    \infer[wire]
%    {\seqq{}}
    {\seqq{tmap \xrightarrow{\mbox{wire } v: t} tmap'}}
   {\seqq{tmap' = tmap [type_{vm,v}(t)/id(v)]}}
    \\[1ex]
  \end{gather*}
\end{table}

%\paragraph*{The definition of ${\tt type\_of\_expr}(e)$ }%\zhilin{@Xiaomu, please add the definition here}



semantics of {\hifirrtl} module. 

Let $M$ be a {\hifirrtl} module and $G$ be a module graph. We define how $G=(vm, ct)$ \emph{matches} $M$ as follows: We define the relations $(vm, ct) \xrightarrow[tmap]{s} (vm', ct')$, where $s$ is a statement. Then $G$ matches $M$ if $(vm_{emp}, ct_{emp}) \xrightarrow[tmap]{ss} (vm, ct)$, where $ss$ is the statement sequence of $M$. 

\zhilin{this table to be cleaned}
\begin{table}[t]
  \small
  \begin{gather*}
    \infer[skip]
    {\seqq{(te, s, rs) \xrightarrow{\mbox{skip}} (te, s, rs)}}
    {\seqq{-}}
    \quad
    \inferii[wire]
%    {\seqq{}}
    {\seqq{(te, s, rs) \xrightarrow{\mbox{wire } v: t} (te', s', rs)}}
   {\seqq{te' = te [t/v]}} {\seqq{s' = s [\bot/v]}}
    \\[1ex]
    \inferiii[reg: $\bot$]
    {\seqq{(te, s, rs) \xrightarrow{\mbox{reg } r: t, c} (te', s', rs)}}
   {\seqq{rs(r) = \bot}} {\seqq{te' = te [t/r]}} {\seqq{s' = s[\bot/r]}}
    \quad
    \inferii[reg: $\neq\bot$]
    {\seqq{(te, s, rs) \xrightarrow{\mbox{reg } r: t, c} (te, s', rs)}}
   {\seqq{rs(r) \neq \bot}} {\seqq{s' = s[rs(r)/r]}}
    \\[1ex]
    \inferiv[reg with reset: $\bot$, false]
    {\seqq{(te, s, rs) \xrightarrow{\mbox{reg } r: t, c \mbox{ with: reset => }(e_1, e_2)} (te', s', rs)}}
   {\seqq{rs(r) = \bot}} {\seqq{ s(e_1)= false}}  {\seqq{te' = te [t/r]}} {\seqq{s' = s[\bot/r]}}
    \\[1ex]
    \inferv[reg with reset: $\bot$, true]
    {\seqq{(te, s, rs) \xrightarrow{\mbox{reg } r: t, c \mbox{ with: reset => }(e_1, e_2)} (te', s', rs')}}
   {\seqq{rs(r) = \bot}} {\seqq{ s(e_1)= true}}  {\seqq{te' = te [t/r]}} {\seqq{s' = s[\bot/r]}} {\seqq{rs' = s[s(e_2)/r]}}
    \\[1ex]
    \inferiii[reg with reset: $\neq\bot$, false]
    {\seqq{(te, s, rs) \xrightarrow{\mbox{reg } r: t, c \mbox{ with: reset => }(e_1, e_2)} (te, s', rs)}}
   {\seqq{rs(r) \neq \bot}} {\seqq{ s(e_1)= false}} {\seqq{s' = s[rs(r)/r]}}
    \\[1ex]
    \inferiv[reg with reset: $\neq\bot$, true]
    {\seqq{(te, s, rs) \xrightarrow{\mbox{reg } r: t, c \mbox{ with: reset => }(e_1, e_2)} (te, s', rs')}}
   {\seqq{rs(r) \neq \bot}} {\seqq{ s(e_1)= true}} {\seqq{s' = s[rs(r)/r]}} {\seqq{rs' = rs[s(e_2)/r]}}
    \\[1ex]
    \inferii[node]
    {\seqq{(te, s, rs) \xrightarrow{\mbox{node } v = e} (te', s', rs)}}
   {\seqq{te' = te[te(e)/v]}} {\seqq{s' = s[s(e)/v]}}
    \quad
    \inferii[connect: non-register]
    {\seqq{(te, s, rs) \xrightarrow{v <= e} (te, s', rs)}}
   {\seqq{v \not \in \registers(A)}} {\seqq{s' = s[s(e)/v]}}
    \\[1ex]
    \inferii[connect: register]
    {\seqq{(te, s, rs) \xrightarrow{v <= e} (te, s, rs')}}
   {\seqq{v \in \registers(A)}} {\seqq{rs' = rs[s(e)/v]}}
  \quad
    \infer[invalid]
    {\seqq{(te, s, rs) \xrightarrow{v \mbox{ is invalid}} (te, s', rs)}}
%   {\seqq{rs(v) \neq \bot}} {\seqq{rs' = rs[s(e)/v]}}
   {\seqq{s' = s[0^{wd(te(v))}/v]}}
  \end{gather*}
%  \vspace{-4mm}
  \caption{Operational semantics of LoFIRRTL statements.}
  \label{tab:lofirrtl-sem}
  \vspace{-6mm}
\end{table}



\zhilin{stopped here}

%\hide{
%\begin{definition}[Vertex]
%	A vertex $v$ is a tuple $(\sorts, \width, \datatype)$ where 
%   \begin{itemize}
%   		\item $\inputport$ and $\outputport$ are sets of input and output ports, respectively; 
%		\item {\sorts} specifies the sort of component in the circuit $v$ is standing for; 		
%		\item {\width} is a nonnegative number, specifying the bit width of $v$;
%		\item {\datatype} specifies the \emph{ground} type of $v$;
%		\item {\expr} is only defined for \emph{nodes}, 
%	\end{itemize}
%\end{definition} 
%}

%\tl{I propose we introduce the notion of \emph{hierarchical module graph} as the intermediate step for (flattened) module graph}
%
%
%\begin{definition}
%	A hierarchical module graph is a tuple $\langle M_1, \cdots, M_n\rangle$ where each module $M_i$ has the following components:
%	\begin{itemize}
%		\item a finite set $V_i$ of vertices;
%		\item a finite set $B_i$ of boxes; ($B_i$ and $V_i$ are all pairwise disjoint)
%		\item a finite set $I_i$ of input ports;
%		\item a finite set $O_i$ of output ports;
%		\item a labeling function $Y_i: V_i \rightarrow \{i+1, \cdots, n\}$ that maps each box of the $i$-th graph to an index grater than $i$. That is, if $Y_i(b)=j$, for a box $b$ of graph $M_i$, then $b$ can be viewed as reference to the definition of the graph $M_j$. 
%	\end{itemize}
%\end{definition}
%
%\tl{to decide later how to proceed}

%vertices 对应module中的component, wire, register, node等，每个vertex有type, width, input/output ports属性
%edges从一个vertex的output port连到另外一个vertex的input port

%from David
%The reset signal, reset value and clock inputs need to be fixed when the register is declared. Only the normal input can be connected (and reconnected) with later statements.
%
%So while a register has four inputs, only one of them is handled using the normal connection information.
%
%Further, a node can serve as an abbreviation for an expression. While an expression can depend on any number of input signals, none of them is handled using the normal connection information, because the expression is fixed at the time the node is declared.



%\begin{example}
%For the program in Fig.~\ref{fig:example}, the module graph is $\langle M_1, M_2\rangle$. 
%
%$M_1=(\{clk, rst\}, \{io\}, ...)$ and 
%
%$M_2=(\{clk,rst\}, \{io\}, \{counter, T_7, T_8\}, ...)$
%where $counter$ is vertex, $T_7$ is a vertex, and $T_8$ is vertex.  
%\end{example}
%Note that the types in the range of $ce$ in Definition~\ref{def-cd} are not necessarily ground types, that is, they may be aggregate types, since HiFIRRTL may contain high-level constructs, e.g. vectors and bundles.  





%\subsection{Operational semantics for module graphs}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\subsection{Denotational Semantics of FIRRTL}

A HiFIRRTL program $P$ is mapped to \emph{a set of} module graphs. %, where the box 
We define $\mathcal{G}(P)$.  % G(P): the set of module graphs of  P


We assume a component environment $ce$ which extracts the type information from $P$ %the sequence $ss$ of statements 
%and 
%the connect mapping $cm$ extracts for each component the expression that is connected to the component according to the last-connect semantics. 

Recall that a circuit defined by $P$ consists of multiple modules. 

\begin{itemize}
	\item tg
\end{itemize} 


%A HiFIRRTL module $M$ will be first transformed into a circuit diagram $D_M = (ss, ce, cm)$, where $ss$ is the sequence of statements in $M$, $ce$ is the component environment obtained from the variable declarations in $M$, possibly with the types of some variables missing or partial, 

%and $cm$ is the trivial mapping that maps each component to $\bot$ (Intuitively, this means that the connects of the components have not be determined yet). The missing or partial  types in $ce$ will be completed by the compilation passes during the transformation of $M$ into a LoFIRRTL module. Moreover, the connect mapping $cm$ will be updated by the ExpandWhens compilation pass. Furthermore, the statement sequence $ss$ will be updated by ExpandConnects, ExpandWhens, LowerTypes, and RemoveReset, while they keep unchanged during the other compilation passes.  



%\subsection{Behavioral semantics of modular graphs}
