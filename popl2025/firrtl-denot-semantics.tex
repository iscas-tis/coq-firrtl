%!TEX root = main.tex

Since each component $cnt$ in $M$ is declared, $cnt$ has a type, say $type(cnt)$. Note that $type(cnt)$ may involve implicit widths.  
On the other hand, all the widths $vm$ are explicit. We can construct a function $tmap$ that assigns to each identifier $v \in \range{id}$ a type where all the widths are explicit, by utilizing the information in $vm$ to instantiate the implicit widths in $type(cnt)$ for components $cnt$ in $M$. More precisely, for each $v \in \range{id}$, $tmap(v)$ is of one of the following forms, 
\begin{itemize}
\item $(ground, t)$ such that $t \in \gtypes$, 
%
\item $(vector, t[n])$ such that $t$ is a type where all widths are explicit, 
%
\item $(bundle, f)$ such that $f$ is a field where all widths are explicit. 
\end{itemize}

In the sequel, we first assume that $tmap$ is already constructed and show how to define the semantics of $M$. Later on, we show how to construct $tmap$ from $M$ and $vm$. 

%Let $G= (vm, ct)$ be a module graph. 
The semantics of a statement $s$ in $M$ is defined by a relation $(vm, ct) \xrightarrow[tmap]{s} (vm', ct')$, where $s$ is seen as a modifier of module graphs. Moreover, the relation can be easily extended to sequences of statements, that is, $(vm, ct) \xrightarrow[tmap]{ss} (vm', ct')$, where $ss$ is a sequence of statements. 

Before defining the semantics, let us introduce several (recursive) definitions, $vm\_match(v, ft, vm, vm')$ (see Table~\ref{tab:vm-match}) and $ct\_match(v, ft, ct, ct')$. 

\zhilin{stopped here}
       
%Let $M$ be a {\hifirrtl} module and $G$ be a module graph. We define how $G = (vm, ct)$ \emph{matches} $M$ as follows: We define the relations $(vm, ct) \xrightarrow[tmap]{s} (vm', ct')$, where $s$ is a statement. Then $G$ matches $M$ if $(vm_{emp}, ct_{emp}) \xrightarrow[tmap]{ss} (vm, ct)$, where $ss$ is the statement sequence of $M$. 

%%%%% vm_match
\begin{table}[htbp]
  \small
  \begin{gather*}
   \infer[size\_of\_ftype: ground]
    {\seqq{size\_of\_ftype(ft) = 1}}
    {\seqq{ft = (ground, gt)}}
    \quad
    \\[1ex]
   \inferii[size\_of\_ftype: vector]
    {\seqq{size\_of\_ftype(ft) = m*n}}
    {\seqq{ft = (vector, ft'[m])}} {\seqq{n = size\_of\_ftype(ft')}}
    \quad
    \\[1ex]
   \inferii[size\_of\_ftype: bundle]
    {\seqq{size\_of\_ftype(ft) = n}}
    {\seqq{ft = (bundle, ff)}} {\seqq{n = size\_of\_fields(ff)}}
    \quad
    \\[1ex]
   \infer[size\_of\_fields: nil]
    {\seqq{size\_of\_fields(ff) = 0}}
    {\seqq{f f = nil}}
    \quad
    \\[1ex]
   \inferiii[size\_of\_fields: $\neq$ nil]
    {\seqq{size\_of\_fields(ff) = m+n}}
    {\seqq{f f = \_\ \_\ ft' ff'}} {\seqq{m = size\_of\_ftype(ft')}} {\seqq{n = size\_of\_fields(ff')}}
    \quad
    \\
    \infer[list\_gtype: ground]
    {\seqq{list\_gtype(ft) = [gt]}}
    {\seqq{ft = (ground, gt)}}
    \quad
    \\[1ex]
    \inferii[list\_gtype: vector]
    {\seqq{list\_gtype(ft) = \underbrace{l'\ ++\ \cdots\ ++\ l'}_{m}}}
    {\seqq{ft = (vector, ft'[m])}}    {\seqq{l' = list\_gtype(ft')}}
    \quad
    \\[1ex]
    \inferii[list\_gtype: bundle]
    {\seqq{list\_gtype(ft) = l'}}
    {\seqq{ft = (bundle, ff)}}    {\seqq{l' = list\_gtype\_fields(ff)}}
    \quad
    \\[1ex]    
    \infer[list\_gtype\_fields: nil]
    {\seqq{list\_gtype\_fields(ff) = []}}
    {\seqq{f f = nil}}
    \quad
    \\[1ex]
    \inferiii[list\_gtype\_fields: $\neq$ nil]
    {\seqq{list\_gtype\_fields(ff) = l'\ {++}\ l''}}
    {\seqq{f f = \_\ \_\ ft'\ ff'}} 
    {\seqq{list\_gtype(ft') = l'}} {\seqq{list\_gtype\_fields(ff') = l''}}
    \quad
  \end{gather*}
%  \vspace{-4mm}
  \caption{$size\_of\_ftype$, $size\_of\_fields$, $list\_gtype$, and $list\_gtype\_fields$}
  \label{tab:size-list-ftype-fields}
\end{table}


\hide{
\begin{table}[htbp]
  \small
  \begin{gather*}
   \infer[size\_of\_ftype: ground]
    {\seqq{size\_of\_ftype(ft) = 1}}
    {\seqq{ft = (ground, gt)}}
    \quad
    \\[1ex]
   \inferii[size\_of\_ftype: vector]
    {\seqq{size\_of\_ftype(ft) = m*n}}
    {\seqq{ft = (vector, ft'[m])}} {\seqq{n = size\_of\_ftype(ft')}}
    \quad
    \\[1ex]
   \inferii[size\_of\_ftype: bundle]
    {\seqq{size\_of\_ftype(ft) = n}}
    {\seqq{ft = (bundle, ff)}} {\seqq{n = size\_of\_fields(ff)}}
    \quad
    \\[1ex]
   \infer[size\_of\_fields: nil]
    {\seqq{size\_of\_fields(ff) = 0}}
    {\seqq{f f = nil}}
    \quad
    \\[1ex]
   \inferiii[size\_of\_fields: $\neq$ nil]
    {\seqq{size\_of\_fields(ff) = m+n}}
    {\seqq{f f = \_\ \_\ ft' ff'}} {\seqq{m = size\_of\_ftype(ft')}} {\seqq{n = size\_of\_fields(ff')}}
    \quad
    \\
    \infer[list\_gtype: ground]
    {\seqq{list\_gtype(ft) = [gt]}}
    {\seqq{ft = (ground, gt)}}
    \quad
    \\[1ex]
    \inferii[list\_gtype: vector]
    {\seqq{list\_gtype(ft) = \underbrace{l'\ ++\ \cdots\ ++\ l'}_{m}}}
    {\seqq{ft = (vector, ft'[m])}}    {\seqq{l' = list\_gtype(ft')}}
    \quad
    \\[1ex]
    \inferii[list\_gtype: bundle]
    {\seqq{list\_gtype(ft) = l'}}
    {\seqq{ft = (bundle, ff)}}    {\seqq{l' = list\_gtype\_fields(ff)}}
    \quad
    \\[1ex]    
    \infer[list\_gtype\_fields: nil]
    {\seqq{list\_gtype\_fields(ff) = []}}
    {\seqq{f f = nil}}
    \quad
    \\[1ex]
    \inferiii[list\_gtype\_fields: $\neq$ nil]
    {\seqq{list\_gtype\_fields(ff) = l'\ {++}\ l''}}
    {\seqq{f f = \_\ \_\ ft'\ ff'}} 
    {\seqq{list\_gtype(ft') = l'}} {\seqq{list\_gtype\_fields(ff') = l''}}
    \quad
  \end{gather*}
%  \vspace{-4mm}
  \caption{$type\_of\_expr$}
  \label{tab:type-expr}
\end{table}
}

\begin{table}[t]
  \small
  \begin{gather*}
    \infer[update\_vm]
    {update\_vm(id, vm, ft, vm')}
    {
    \begin{array}{r l l}
    \multicolumn{3}{c}{id = (v, m) \wedge size\_of\_ftype(ft) = sz \wedge list\_gtype(ft) = ls} \\
    \forall n.\ 0 \le n < sz & \Rightarrow & vm'(v, m+n) = ls[n] \\
    \forall v', n.\ (v' \neq v \vee n' < m \vee n' \ge m+sz) & \Rightarrow & vm(v', n') = vm'(v', n') \\
    \end{array}
    }
    \\[1ex]
    \infer[update\_ct]
    {update\_ct(id, ct, ft, ls, ct')}
    {
    \begin{array}{r l l}
    \multicolumn{3}{c}{id = (v, m) \wedge size\_of\_ftype(ft) = sz} \\
    \forall n.\ 0 \le n < sz & \Rightarrow & ct'(v, m+n) = ls[n] \\
    \forall v', n.\ (v' \neq v \vee n' < m \vee n' \ge m+sz) & \Rightarrow & ct(v', n') = ct'(v', n') \\
    \end{array}
    }
    \\[1ex]
    \infer[update\_ct\_ref]
    {update\_ct\_ref(id, ct, ft, ls, ft', ls', ct')}
    {
    \begin{array}{r l l}
    \multicolumn{3}{c}{id = (v, m) \wedge size\_of\_ftype(ft) = sz} \\
    \forall n.\ 0 \le n < sz & \Rightarrow & ct'(v, m+n) = ls[n] \\
    \forall v', n.\ (v' \neq v \vee n' < m \vee n' \ge m+sz) & \Rightarrow & ct(v', n') = ct'(v', n') \\
    \end{array}
    }
  \end{gather*}
  \caption{$update\_vm$, $update\_ct$, and $update\_ct\_ref$}
  \label{tab:update-vm-ct}
\end{table}

\zhilin{this table to be cleaned}
\begin{table}[t]
  \small
  \begin{gather*}
    \infer[skip]
    {\seqq{(vm, ct) \xrightarrow[tmap]{\mbox{skip}} (vm, ct)}}
    {\seqq{-}}
    \quad
    \\[1ex]
    \infer[wire]
    {\seqq{(vm, ct) \xrightarrow[tmap]{\mbox{wire } x: t} (vm', ct')}}
   {
    \begin{array}{c}
    id(x) = v \wedge tmap(v)= ft \wedge size\_of\_ftype(ft) = sz \\
    \forall n.\ 0 \le n < sz \Rightarrow vm(v, n) = none \wedge ct(v, n) = none  \\
%    \forall n.\ n \ge size\_of\_ftype(ft) \Rightarrow vm(v, n) =  vm'(v,n)\\
%    \forall n.\ 0 \le n < size\_of\_ftype(ft) \Rightarrow ct(v, n) = none \wedge ct'(v,n) = undefined \\
    update\_vm((v, 0), vm, ft, vm') \wedge update\_ct((v, 0), ct, ft, [\underbrace{undefined, \ldots, undefined}_{m}], ct')
    \end{array}
   }
    \quad
    \\[1ex]
    \infer[node]
    {\seqq{(vm, ct) \xrightarrow[tmap]{\mbox{node } x = e} (vm', ct')}}
    {
    \begin{array}{c}
   id(x) = v \wedge type\_of\_exp(e, tmap)= ft \wedge size\_of\_ftype(ft) = sz \wedge list\_gexp(e, ft) = ls \\
    \forall n.\ 0 \le n < sz  \Rightarrow  vm(v, n) = none \wedge ct(v, n) = none \\
    update\_vm((v, 0), vm, ft, vm') \wedge update\_ct((v, 0), ct, ft, ls, ct')
    \end{array}    
    }
    \quad
    \\[1ex]
    \infer[connect: exp]
    {\seqq{(vm, ct) \xrightarrow[tmap]{r <= e} (vm', ct')}}
    {
    \begin{array}{c}
    vm = vm' \wedge id(r)=(v, m) \wedge type\_of\_ref(r, tmap)= rft \ \wedge \\
     size\_of\_ftype(rft) = sz \wedge type\_of\_exp(e, tmap)= eft \\
    connect\_type\_compatible(rft, eft, false) \\
    \forall n.\ 0 \le n < sz \Rightarrow ct(v, m+n) \neq none \\
%    \forall n.\ n \ge size\_of\_ftype(ft) \Rightarrow vm(v, n) =  vm'(v,n)\\
%    \forall n.\ 0 \le n < size\_of\_ftype(ft) \Rightarrow ct(v, n) = none \wedge ct'(v,n) = undefined \\
     update\_ct((v, m), ct, rft, list\_gexp(e, eft), ct')
    \end{array}    
    }
    \quad
    \\[1ex]
    \infer[connect: ref]
    {\seqq{(vm, ct) \xrightarrow[tmap]{r <= r'} (vm', ct')}}
    {
    \begin{array}{c}
    vm = vm' \wedge id(r)=(v, m) \wedge type\_of\_ref(r, tmap)= ft \ \wedge \\
     size\_of\_ftype(ft) = sz \wedge type\_of\_ref(r', tmap)= ft' \\
    connect\_type\_compatible(ft, ft', false) \\
    \forall n.\ 0 \le n < sz \Rightarrow ct(v, m+n) \neq none \\
%    \forall n.\ n \ge size\_of\_ftype(ft) \Rightarrow vm(v, n) =  vm'(v,n)\\
%    \forall n.\ 0 \le n < size\_of\_ftype(ft) \Rightarrow ct(v, n) = none \wedge ct'(v,n) = undefined \\
     update\_ct\_ref((v, m), ct, ft, list\_ref(r, tmap), ft', list\_ref(r', tmap), ct')
    \end{array}    
    }
  \end{gather*}
%  \vspace{-4mm}
  \caption{Denotational Semantics of {\hifirrtl} statements}
  \label{tab:firrtl-sem}
\end{table}


\hide{
For a statement $\kappa\ v: t$ where $\kappa =\mbox{\prettyll{input},\prettyll{output},\prettyll{wire}}$, 
We define a mapping $tmap$ that assigns to each identifier from $\range{id}$ a type where all widths are explicit.    
For each $v \in \dom(id)$, 
\begin{itemize}
\item if $v$ is declared in a statement $p \equiv \{\mbox{\prettyll{input}} v: t$, then 
\begin{itemize}
\item if $t = \theta$ with $\theta \in \{\mbox{\prettyll{Clock}}, \mbox{\prettyll{Reset}} \mbox{\prettyll{AsyncReset}}\}$ and $vm((id(v),0)) = (\inport, \theta)$, then $tmap(id(v)) = (gt, \theta)$, 
%
\item if $t = \theta$ with $\theta \in \{\mbox{\prettyll{UInt}}, \mbox{\prettyll{SInt}}\}$ and $vm((id(v), 0)) = (\inport, (\theta, n))$, then $tmap(id(v)) = (gt, \theta \langle n \rangle)$, 
%
\item if $t = \theta\langle n \rangle$ with $\theta \in \{\mbox{\prettyll{UInt}}, \mbox{\prettyll{SInt}}\} $ and $vm((id(v),0)) = (\inport, (\theta, n))$, then $tmap(id(v)) = (gt, \theta \langle n \rangle)$, 
%
\item if $t = t'[n]$ and $vm((id(v),n)) = (\inport, )$, then $tmap(id(v)) = (gt, \theta \langle n \rangle)$
\end{itemize}
\item if $v$ is declared in a statement $p \equiv \{\mbox{\prettyll{output}} v: t$, then $tmap(id(v))$ is defined similarly to the case $p \equiv \{\mbox{\prettyll{input}} v: t$, except that $\inport$ is replaced by $\outport$, 
%   
\item if $v$ is declared in a statement $s \equiv \mbox{\prettyll{wire} } v: t$, then 
\begin{itemize}
\item if $v$ is declared in a statement $p \equiv \{\mbox{\prettyll{input}} v: t$, then 
\begin{itemize}
\item if $t = \theta$ with $\theta \in \{\mbox{\prettyll{Clock}}, \mbox{\prettyll{Reset}} \mbox{\prettyll{AsyncReset}}\}$ and $vm(id(v)) = (\inport, \theta)$, then $tmap(id(v)) = (gt, \theta)$, 
%
\item if $t = \theta$ with $\theta \in \{\mbox{\prettyll{UInt}}, \mbox{\prettyll{SInt}}\}$ and $vm(id(v)) = (\inport, (\theta, n))$, then $tmap(id(v)) = (gt, \theta \langle n \rangle)$. 
\end{itemize}
\end{itemize}
\end{itemize}
}




A HiFIRRTL program $P$ is mapped to \emph{a set of} module graphs. %, where the box 
We define $\mathcal{G}(P)$.  % G(P): the set of module graphs of  P


We assume a component environment $ce$ which extracts the type information from $P$ %the sequence $ss$ of statements 
%and 
%the connect mapping $cm$ extracts for each component the expression that is connected to the component according to the last-connect semantics. 

Recall that a circuit defined by $P$ consists of multiple modules. 

\begin{itemize}
	\item tg
\end{itemize} 


%A HiFIRRTL module $M$ will be first transformed into a circuit diagram $D_M = (ss, ce, cm)$, where $ss$ is the sequence of statements in $M$, $ce$ is the component environment obtained from the variable declarations in $M$, possibly with the types of some variables missing or partial, 

%and $cm$ is the trivial mapping that maps each component to $\bot$ (Intuitively, this means that the connects of the components have not be determined yet). The missing or partial  types in $ce$ will be completed by the compilation passes during the transformation of $M$ into a LoFIRRTL module. Moreover, the connect mapping $cm$ will be updated by the ExpandWhens compilation pass. Furthermore, the statement sequence $ss$ will be updated by ExpandConnects, ExpandWhens, LowerTypes, and RemoveReset, while they keep unchanged during the other compilation passes.  



%\subsection{Behavioral semantics of modular graphs}
